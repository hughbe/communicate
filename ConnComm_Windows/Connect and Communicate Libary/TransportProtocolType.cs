using System;

namespace ConnComm
{
    /// <summary>
    /// The enum that dictates what type of protocol the server will use to publish itself on the network. It is currently limited to TCP only.
    /// </summary>
    public enum TransportProtcolType
    {
        TCP
    }
}
